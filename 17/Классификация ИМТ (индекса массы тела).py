# Классификация ИМТ (индекса массы тела): напишите программу,
# которая запрашивает у пользователя его вес и рост, вычисляет ИМТ и
# выводит соответствующую категорию: недостаточный вес (ИМТ < 18.5),
# нормальный вес (ИМТ 18.5-24.9), избыточный вес (ИМТ 25-29.9) или
# ожирение (ИМТ >= 30).

простой пример программы на Python, которая запрашивает рост и вес, вычисляет ИМТ и выводит категорию.

```python
# Рекомендуемые единицы: вес в килограммах, рост в сантиметрах
def calculate_bmi(weight_kg, height_cm):
    height_m = height_cm / 100.0
    bmi = weight_kg / (height_m * height_m)
    return bmi

def bmi_category(bmi):
    if bmi < 18.5:
        return "Недостаточный вес"
    elif bmi < 25.0:
        return "Нормальный вес"
    elif bmi < 30.0:
        return "Избыточный вес"
    else:
        return "Ожирение"

def main():
    try:
        weight = float(input("Введите вес (кг): ").replace(',', '.'))
        height = float(input("Введите рост (см): ").replace(',', '.'))
    except ValueError:
        print("Неверный ввод. Пожалуйста, введите числовые значения.")
        return

    bmi = calculate_bmi(weight, height)
    category = bmi_category(bmi)

    print(f"Ваш ИМТ: {bmi:.1f}")
    print(f"Категория: {category}")

if __name__ == "__main__":
    main()
```

Пояснения:
- ИМТ рассчитывается как вес (кг) делить на квадрат роста (м): \( \text{BMI} = \frac{weight}{(height/100)^2} \).
- Категории соответствуют указанным диапазонам:
  - менее 18.5 → Недостаточный вес
  - 18.5–24.9 → Нормальный вес
  - 25.0–29.9 → Избыточный вес
  - 30.0 и выше → Ожирение

